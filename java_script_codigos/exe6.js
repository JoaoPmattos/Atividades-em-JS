let A = parseFloat(prompt('insira um valor para A:'));
let B = parseFloat(prompt('insira o valor para B:'));
let X = (A + B);

if (X => 10) {
    console.log(`a soma dos valores ${A},e,${B},foi = ${X}`);
    console.log('Maior ou igual a 10!');
}
else {
    console.log('a soma dos valores não foi maior que 10!');
}